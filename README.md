# 🚗 Система симуляции движения транспортного средства

Веб-приложение для построения детальных маршрутов с симуляцией движения транспортного средства в реальном времени.

## ✨ Возможности

- 🗺️ **Интерактивная карта** - Leaflet + OpenStreetMap
- 📍 **Поиск по адресу** - геокодирование через Nominatim API
- 🛣️ **Реальные маршруты** - построение по дорогам через OSRM
- 📊 **100,000 точек** - детальный маршрут с высотами и уклонами
- 🚗 **Симуляция движения** - анимация ТС по маршруту
- 📈 **Графики в реальном времени** - высота, уклон, скорость
- ⚡ **Оптимальная скорость** - рекомендации на основе уклона
- ⛽ **Расчет расхода** - текущий и общий расход топлива
- ⏱️ **Виртуальное время** - время в пути с учетом скорости
- 🎮 **Множитель скорости** - от 1x до 100x для ускорения симуляции

## 🎯 Скриншоты

### Главный интерфейс
- Карта с маршрутом
- Боковая панель с показателями
- Графики высоты, уклона и скорости

### Симуляция движения
- Анимация ТС по маршруту
- Пройденный путь (зеленая линия)
- Обновление графиков в реальном времени

## 🚀 Быстрый старт

### Требования

- Python 3.10+
- Docker Desktop (для OSRM)
- 8+ GB RAM
- 15+ GB свободного места

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/Jikibo/Building_a_route_between_two_points_with_division_into-waypoints.git
cd Building_a_route_between_two_points_with_division_into-waypoints
```

2. **Установите зависимости:**
```bash
pip install flask flask-cors pandas requests numpy rasterio polyline tqdm
```

3. **Подготовьте OSRM** (см. раздел "Настройка OSRM")

4. **Запустите сервер:**
```bash
python server.py
```

5. **Откройте браузер:**
```
http://localhost:8001
```

## 🐳 Настройка OSRM

### Скачивание данных

Скачайте OSM данные для нужного региона:
- Россия: https://download.geofabrik.de/russia-latest.osm.pbf

### Подготовка данных

```powershell
# Перейдите в папку с данными
cd OSRM_local

# Extract
docker run --rm -t -v ${PWD}:/data osrm/osrm-backend osrm-extract -p /opt/car.lua /data/russia-latest.osm.pbf

# Partition (для MLD)
docker run --rm -t -v ${PWD}:/data osrm/osrm-backend osrm-partition /data/russia-latest.osrm

# Customize
docker run --rm -t -v ${PWD}:/data osrm/osrm-backend osrm-customize /data/russia-latest.osrm
```

### Запуск OSRM

```powershell
docker run -d --name osrm-russia -p 5000:5000 -v C:\path\to\OSRM_local:/data osrm/osrm-backend osrm-routed --algorithm mld /data/russia-latest.osrm
```

**Замените** `C:\path\to\OSRM_local` на ваш путь!

### Проверка

```powershell
Invoke-WebRequest -Uri "http://localhost:5000/route/v1/driving/37.6173,55.7558;40.4093,56.1366?overview=false" -UseBasicParsing
```

Должны увидеть JSON с `"code":"Ok"`

## 📊 Использование

### 1. Построение маршрута

1. Введите начальный адрес (например: "Москва")
2. Нажмите "Найти"
3. Введите конечный адрес (например: "Владимир")
4. Нажмите "Найти"
5. Нажмите "Построить маршрут"
6. Подождите 2-5 минут

### 2. Запуск симуляции

1. Установите множитель скорости (рекомендуется 50x для теста)
2. Нажмите "▶ Старт"
3. Наблюдайте за движением ТС

### 3. Управление симуляцией

- **▶ Старт** - запустить/продолжить
- **⏸ Пауза** - приостановить
- **⏹ Сброс** - вернуться в начало
- **Ползунок** - изменить скорость симуляции (1x-100x)

## 📈 Показатели

### Текущие показатели
- **Скорость** - текущая скорость ТС (км/ч)
- **Время в пути** - виртуальное время (учитывает множитель)
- **Пройдено** - пройденное расстояние (км)
- **Текущий расход** - расход топлива (л/100км)

### Графики
- **Профиль высот** - изменение высоты по маршруту
- **Уклон дороги** - подъемы и спуски (%)
- **Скорость движения** - история скорости

### Оптимальная скорость
- **Рекомендуемая** - оптимальная скорость для текущего участка
- **Причина** - обоснование (уклон, условия)

### Параметры ТС
- **Масса ТС** - масса транспортного средства (кг)
- **Масса груза** - масса перевозимого груза (кг)
- **Общая масса** - суммарная масса (кг)

## ⚙️ Настройка

### Параметры ТС

Отредактируйте `vehicle_data.json`:

```json
{
  "vehicle": {
    "vehicle_mass_kg": 3500,
    "cargo_mass_kg": 2000,
    "max_speed_kmh": 90,
    "fuel_consumption_base_l_per_100km": 12.0
  },
  "simulation": {
    "acceleration_kmh_per_sec": 5.0,
    "deceleration_kmh_per_sec": 8.0,
    "time_multiplier": 1.0
  }
}
```

### Множитель скорости

- **1x** - реальное время (185 км = 3 часа)
- **10x** - в 10 раз быстрее (185 км = 18 минут)
- **50x** - в 50 раз быстрее (185 км = 3.6 минуты)
- **100x** - в 100 раз быстрее (185 км = 1.8 минуты)

## 🧮 Алгоритмы

### Расчет скорости
```
Целевая скорость зависит от уклона:
- Крутой подъем (>5%): снижение на 5 км/ч за каждый %
- Подъем (2-5%): 80% от максимальной
- Нормально (-2% до +2%): 70% от максимальной
- Спуск (-2% до -5%): 85% от максимальной
- Крутой спуск (<-5%): снижение на 3 км/ч за каждый %
```

### Расчет расхода
```
Расход = БазовыйРасход × (1 + (Скорость-60)×0.01) × (1 + Уклон×0.05)

Примеры:
- 60 км/ч, 0%: 12.0 л/100км
- 80 км/ч, 0%: 14.4 л/100км
- 60 км/ч, 5%: 15.0 л/100км
- 80 км/ч, 5%: 18.0 л/100км
```

### Расчет уклона
```
Градиент = (ΔВысота / ΔРасстояние) × 100%

где:
ΔВысота - разница высот между точками (м)
ΔРасстояние - расстояние между точками (м)
```

## 📁 Структура проекта

```
.
├── server.py                          # Flask API сервер
├── index.html                         # Веб-интерфейс
├── vehicle_data.json                  # Параметры ТС
├── build_route_100k_and_sample.py     # Генератор маршрутов
├── requirements.txt                   # Python зависимости
├── appRasterSelectAPI...tif           # DEM файл (высоты)
└── route_*.csv                        # Сгенерированные маршруты
```

## 🔧 Технологии

### Frontend
- HTML5 + CSS3 + JavaScript (ES6+)
- Leaflet.js 1.9.4 (карта)
- Chart.js 4.4.0 (графики)

### Backend
- Python 3.10+
- Flask 2.3+ (веб-сервер)
- Pandas (обработка данных)
- Rasterio (чтение DEM)

### External Services
- OSRM (роутинг)
- Nominatim (геокодирование)
- OpenStreetMap (тайлы карты)

## 📊 Производительность

### Генерация маршрута
- 10,000 точек: ~30 секунд
- 100,000 точек: ~3-5 минут

### Симуляция
- CPU: 5-10%
- RAM: ~200 MB
- FPS: 10 (обновление каждые 100мс)

### Оптимизации
- Кэширование маршрутов
- Кластеризация точек (4 уровня)
- Батчинг DEM sampling
- Накопительные графики

## 🎓 Примеры маршрутов

### Короткий (тест)
- **Откуда:** Москва, Красная площадь
- **Куда:** Москва, МГУ
- **Расстояние:** ~25 км
- **Время:** ~30 минут (1x)
- **Точек:** 10,000

### Средний
- **Откуда:** Москва
- **Куда:** Владимир
- **Расстояние:** ~185 км
- **Время:** ~3 часа (1x)
- **Точек:** 100,000

### Длинный
- **Откуда:** Санкт-Петербург
- **Куда:** Москва
- **Расстояние:** ~705 км
- **Время:** ~12 часов (1x)
- **Точек:** 100,000

## ❓ FAQ

### Почему расход показывает 0.0?
Убедитесь что файл `vehicle_data.json` загружен. Проверьте консоль браузера (F12).

### Как ускорить симуляцию?
Используйте ползунок "Скорость симуляции". Рекомендуется 50x для тестирования.

### Графики не заполняются?
Это нормально! Графики заполняются по мере движения ТС.

### OSRM не работает?
Проверьте что Docker контейнер запущен: `docker ps`

### Маршрут строится долго?
Это нормально для 100,000 точек. Основное время уходит на получение высот из DEM.

## 🐛 Известные ограничения

- Упрощенная физика (нет инерции, трения)
- Нет учета пробок и светофоров
- Расход топлива приблизительный (±20-30%)
- Требуется локальный OSRM для больших маршрутов

## 📝 Лицензия

MIT License

## 👥 Авторы

- Разработка: Jikibo
- GitHub: https://github.com/Jikibo

## 🙏 Благодарности

- OpenStreetMap - карты
- OSRM - роутинг
- Leaflet - библиотека карт
- Chart.js - графики
- Nominatim - геокодирование

---

**Готовы начать?** Запустите `python server.py` и откройте http://localhost:8001! 🚀
