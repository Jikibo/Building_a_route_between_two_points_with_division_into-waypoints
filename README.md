# Building_a_route_between_two_points_with_division_into-waypoints
Build a route between two points, splitting it into waypoints. The number of split points can be changed, and the limit on request is removed by deploying a local OSRM.
